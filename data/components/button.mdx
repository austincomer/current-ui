---
metaTitle: Button - Current UI
metaDescription: Displays a button or a component that looks like a button.
name: Button
---

<FlexGroup>
  # Button

  <Description
    hasRadixLink={false}
  >
    Displays a button or a component that looks like a button.
  </Description>
</FlexGroup>

<PreviewComponent
  componentName='Button'
  demoComponent={<ButtonDemo />}
  demoCodePath='ButtonDemo'
/>

<FlexGroup>
  ## Installation

Copy and paste the following code into your project.

<ComponentSource code='Button' />

Update the import paths to match your project setup.

## Anatomy

Import all parts and piece them together.

<CodeBlock code={`import { buttonVariants } from "@/components/ui/button"`} />

<CodeBlock
  code={`<Link className={buttonVariants({ variant: "outline" })}>Click here</Link>`}
/>
</FlexGroup>

<FlexGroup>
## Props

### Size

<ButtonProps
  variantName='variant'
  variantProps={[
    'default',
    'secondary',
    'outline',
    'ghost',
    'link',
    'destructive',
  ]}
/>

### Shape

<ButtonProps variantName='shape' variantProps={['square', 'rounded', 'pill']} />

### Size

<ButtonProps
  variantName='size'
  variantProps={['default', 'xs', 'sm', 'lg', 'xl', 'icon']}
/>

</FlexGroup>
